import { Routes } from '@angular/router';
import { MediaUploadComponent } from './components/media-upload/media-upload.component';

export const mediaRoutes: Routes = [
  { path: '', component: MediaUploadComponent }
];
